# --- Production Dependencies ---
openreview-py==2.0.1  # Pin your version for reproducibility
requests>=2.28
# typing is usually included in modern Python, but listing is safe for older envs
typing_extensions>=4.0 

# --- Development/CI Dependencies ---
# These are needed by the GitHub Actions setup:
ruff>=0.1.0
black>=23.0
pytest>=7.0
pytest-cov>=4.0